# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
web-build

# Expo
.expo
expo-env.d.ts

# Native prebuild output — regenerated from app.json, never hand-edited
android
ios

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets and local project links
.env
.env.local

# Points an agent at the hosted MCP server and carries a live cellar token.
.mcp.json
.firebase
.firebaserc

plan.md
feature_ideas.md

# Web static output — generated from mcp/skill by scripts/sync-skill.mjs
public

# Supabase CLI scratch — the linked project ref, written on deploy
supabase/.temp/
